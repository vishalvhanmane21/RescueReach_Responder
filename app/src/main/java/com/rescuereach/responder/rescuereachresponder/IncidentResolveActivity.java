package com.rescuereach.responder.rescuereachresponder;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

import com.rescuereach.responder.rescuereachresponder.R;

public class IncidentResolveActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_incident_resolve);
    }
}
